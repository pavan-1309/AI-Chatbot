{
  "name": "ai-chatbot-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.450.0",
    "amazon-cognito-identity-js": "^6.3.7",
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^9.1.0"
  },
  "scripts": {
    "prebuild": "node fetch-config.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && aws s3 sync build/ s3://YOUR_BUCKET_NAME --delete"
  },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
